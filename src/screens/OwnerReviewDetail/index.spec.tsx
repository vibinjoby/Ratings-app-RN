import 'react-native'
import React from 'react'
import OwnerReviewDetail from '.'

import { render } from '@testing-library/react-native'

describe('OwnerReviewDetail', () => {
    it('should render', () => {
        render(<OwnerReviewDetail />)
    })
})